# Optional overrides after deploy (Render Dashboard → karakeep → Environment).
# Most variables are wired automatically by render.yaml via fromService / generateValue.

# AI tagging and summarization — set after deploy; not required at Blueprint Apply time
# OPENAI_API_KEY=

# See https://docs.karakeep.app/configuration/environment-variables for the full list
